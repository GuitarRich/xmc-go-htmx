package components

import (
	"github.com/guitarrich/headless-go-htmx/model"
)

templ RenderPartialDesignDynamicPlaceholder(placeholderKey string, props model.PlaceholderComponent) {
	{ Log("PartialDesignDynamicPlaceholderComponent") }
	@RenderPlaceholder(props.Params.Sig, props.Placeholders)
}
